# Copy to .env (Vite reads VITE_* at build time). Never commit .env.
VITE_API_URL=http://localhost:8080
VITE_WS_URL=ws://localhost:8080

# No API key is shipped to the browser. Responders sign in at /login and
# the console uses their short-lived access token.
